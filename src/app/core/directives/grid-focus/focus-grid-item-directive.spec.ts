import { FocusGridItemDirective } from './focus-grid-item-directive';

describe('FocusGridItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusGridItemDirective();
    expect(directive).toBeTruthy();
  });
});
