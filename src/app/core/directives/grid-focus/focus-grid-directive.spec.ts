import { FocusGridDirective } from './focus-grid-directive';

describe('FocusGridDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusGridDirective();
    expect(directive).toBeTruthy();
  });
});
