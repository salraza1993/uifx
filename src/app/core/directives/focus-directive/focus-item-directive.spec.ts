import { FocusItemDirective } from './focus-item-directive';

describe('FocusItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusItemDirective();
    expect(directive).toBeTruthy();
  });
});
