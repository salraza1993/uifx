import { Component } from '@angular/core';

@Component({
  selector: 'sr-scroll-progress, scroll-progress',
  imports: [],
  template: '',
  styleUrl: './scroll-progress.css',
  host: {
    class: 'scroll-progress-host'
  }
})
export class ScrollProgress {}
